import { LightningElement} from 'lwc';

export default class TextAreaCMP extends LightningElement {
/*@api richTextInputValue = "";

handleInputChange(event) {
var richtextvalue = event.target.value;
const inputChangeEvent = new CustomEvent('richtextinputchange', {
detail: { richtextvalue },
});
// Fire the custom event
this.dispatchEvent(inputChangeEvent);
}*/
}